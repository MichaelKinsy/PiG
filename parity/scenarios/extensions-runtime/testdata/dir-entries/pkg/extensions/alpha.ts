// One file of a pi.extensions directory entry: its own extension.
export default function (pi) {
  pi.registerCommand("alpha-entry", { description: "alpha directory entry", handler: async () => {} });
}
