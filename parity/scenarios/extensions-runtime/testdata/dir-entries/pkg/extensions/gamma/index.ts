// A subdirectory of a pi.extensions directory entry loads through its index.
export default function (pi) {
  pi.registerCommand("gamma-entry", { description: "gamma directory entry", handler: async () => {} });
}
