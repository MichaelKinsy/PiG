# Upstream evidence: Pi's pinned model-selector.ts snapshot, probed with fake GEMINI_API_KEY and
# GOOGLE_API_KEY on 2026-09-25 at 100x40, contains every Google model plus the
# two local fixture models. GOOGLE_API_KEY is deliberately not an auth alias.
# Compare the entire list surface through the dim action hint, including the
# independent upstream catalog count, current/default markers, and refresh status.
name = "15-model-picker-google-and-hint"
description = "Google auth reaches the model picker and the complete styled action hint matches Pi"
covers = [
  "packages/coding-agent/src/modes/interactive/components/model-selector.ts",
  "packages/coding-agent/src/modes/interactive/interactive-mode.ts",
]
tags = ["fast", "hermetic", "tui"]
driver = "interactive-tmux"

[tmux]
width = 100
height = 40
# Quiet startup has no banner; use the same footer/prompt readiness contract as Pi.
ready_pattern_pig = "(auto)"
ready_pattern_pi = "(auto)"
ready_timeout_seconds = 20
keys = ["/model", "Enter"]
settle_seconds = 2
capture_start = "Only showing models"
capture_end = "to cancel"

[env]
pig = ["PIG_CODING_AGENT_DIR=testdata/model-picker-defaults", "GEMINI_API_KEY=fake-gemini", "GOOGLE_API_KEY=ignored-google"]
pi = ["PI_CODING_AGENT_DIR=testdata/model-picker-defaults", "GEMINI_API_KEY=fake-gemini", "GOOGLE_API_KEY=ignored-google", "PI_SKIP_VERSION_CHECK=1"]
pig_args = ["--no-extensions", "--offline", "--session-dir", "{{TEMP}}/sessions"]
pi_args = ["--no-extensions", "--offline", "--session-dir", "{{TEMP}}/sessions"]

[assert]
both_reach_ready = true
both_contain = ["[google]", "[fixture]", "Enter to select · Ctrl+S to set as default · Escape/Ctrl+C to cancel"]
output_equal = true
escaped_output_equal = true
runs = 3
