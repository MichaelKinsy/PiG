# Behavior family: settings
# Upstream evidence: SelectSubmenu.applyFilter rebuilds the SelectList for an empty result. SelectList.render returns the unpadded noMatch row, and getSelectListTheme colors it with muted foreground, not dim intensity. Probed the pinned installed SelectList and this complete model-settings path. The old Pig row used SGR 2 and failed escaped equality.
name = "08-settings-model-thinking-empty-search"
description = "The model-thinking submenu preserves exact empty-search rendering and cannot confirm an absent model."
covers = [
  "packages/coding-agent/src/modes/interactive/components/settings-submenu.ts",
  "packages/tui/src/components/select-list.ts",
]
tags = ["fast", "hermetic", "tui"]
driver = "interactive-tmux"
model = "fixture/thinking-model"

[tmux]
width = 100
height = 35
ready_pattern_pig = "(auto)"
ready_pattern_pi = "$0.000"
ready_timeout_seconds = 25
capture_start = "Per-Model Thinking Level"
capture_start_last = true
capture_end = "Type to filter · Enter to select · Esc to go back"

[[tmux.steps]]
keys = ["/settings", "Enter"]
wait_contains = ["Type to search · Enter/Space to change · Esc to cancel"]
wait_timeout_seconds = 5
settle_seconds = 1

[[tmux.steps]]
keys = ["Default thinking level per model", "Enter"]
wait_contains = ["Per-Model Thinking Level", "Step 1/2 · Select a model to configure"]
wait_timeout_seconds = 5
settle_seconds = 1

[[tmux.steps]]
keys = ["absent-model-xyz", "Enter"]
wait_contains = ["Per-Model Thinking Level", "No matching commands"]
wait_timeout_seconds = 5
settle_seconds = 1

[env]
pig = ["PIG_CODING_AGENT_DIR=testdata/model-agent"]
pi = ["PI_CODING_AGENT_DIR=testdata/model-agent"]
pig_args = ["--no-extensions", "--offline", "--session-dir", "{{TEMP}}/sessions"]
pi_args = ["--no-extensions", "--offline", "--session-dir", "{{TEMP}}/sessions"]

[assert]
both_reach_ready = true
both_not_contain = ["unknown command", "panic:", "goroutine "]
output_equal = true
escaped_output_equal = true
runs = 3
