{
  "name": "chain",
  "version": "0.1.0",
  "description": "Pi extension: run a task as a chain of agent steps (scout -> plan -> build -> test) with a live progress widget",
  "type": "module",
  "keywords": ["pi-package", "pi", "pi-coding-agent", "extension"],
  "license": "MIT",
  "pi": { "extensions": ["./index.ts"] },
  "peerDependencies": { "@earendil-works/pi-coding-agent": "*" }
}
