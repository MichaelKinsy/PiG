[package]
name = "pig-sdk"
version = "0.1.0"
edition = "2024"
description = "Rust SDK for building pig subprocess extensions"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["preserve_order"] }
